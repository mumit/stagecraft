{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:stagecraft:schema:stage-01",
  "title": "Stage 01 Requirements Gate",
  "description": "Stage 1 (Requirements) gate. Authored by the pm agent after writing pipeline/brief.md. See .devteam/rules/pipeline.md §Stage 1 for the full contract.",
  "type": "object",
  "required": [
    "acceptance_criteria_count",
    "out_of_scope_items",
    "required_sections_complete"
  ],
  "properties": {
    "acceptance_criteria_count": {
      "type": "integer",
      "minimum": 0,
      "description": "Number of acceptance criteria in the brief. Stage 6 maps these 1:1 to passing tests; mismatch blocks the Stage 7 auto-fold."
    },
    "out_of_scope_items": {
      "type": "array",
      "description": "Things the brief explicitly excludes; reviewers and PM use this to push back on scope creep."
    },
    "required_sections_complete": {
      "type": "boolean",
      "description": "True when the brief contains every section the brief template requires for the chosen track. See templates/brief-template.md for the canonical blank form."
    },
    "active_roles": {
      "type": ["array", "null"],
      "items": {
        "type": "string",
        "enum": ["backend", "frontend", "platform", "qa", "documentation"]
      },
      "description": "Explicit list of build workstream roles that are in scope for this project. null (or absent) means all standard roles are active. The optional documentation role is valid only as the sole role for a documentation-only change with exact affected_files. The orchestrator filters both build (stage-04) and peer-review (stage-05) dispatch against this list."
    },
    "affected_files": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Exact repo-relative files approved by the brief. Required and semantically validated when active_roles selects the optional documentation workstream; globs and directories are forbidden."
    }
  },
  "additionalProperties": true
}
