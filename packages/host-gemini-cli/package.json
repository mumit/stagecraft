{
  "name": "@devteam/host-gemini-cli",
  "version": "1.0.0",
  "private": true,
  "description": "Stagecraft host adapter for Gemini CLI (deprecated upstream — see README.md).",
  "main": "adapter.js",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
